---
name: Bug Report
about: Report something the plugin does wrong
title: "[Bug] "
labels: bug
assignees: ''
---

## Description

What goes wrong, in a sentence or two.

## Steps to Reproduce

1.
2.
3.

## Expected Behavior

What the plugin was expected to do instead.

## Actual Behavior

What the plugin actually did. Quote the exact command output or notification text where there is
one.

**Do not paste raw IP addresses.** This plugin treats player IP addresses as sensitive data and
encrypts them at rest; redact them (`203.0.113.x`) before posting.

## Environment

- Plugin version (from `/version AlternateAccountFinder` or the jar name):
- Server software and version (Spigot, Paper, …; `/version`):
- Java version (`java -version`):
- Database (H2 by default, or MariaDB):
- Other plugins installed that interact with this one (Mailboxes, RPKit, …):

## Logs

Relevant lines from `logs/latest.log`, including any stack trace. Redact IP addresses.

## Additional Context

Anything else worth knowing — configuration changes, when the behavior started, whether it
survives a restart.
